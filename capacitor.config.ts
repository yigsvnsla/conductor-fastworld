import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.fastworld.conductor',
  appName: 'Fastworld Conductores',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
